<!--
doc/src/sgml/ref/discard.sgml
PostgreSQL documentation
-->

<refentry id="SQL-DISCARD">
 <indexterm zone="sql-discard">
  <primary>DISCARD</primary>
 </indexterm>
 <refmeta>
  <refentrytitle>DISCARD</refentrytitle>
  <manvolnum>7</manvolnum>
  <refmiscinfo>SQL - Language Statements</refmiscinfo>
 </refmeta>

 <refnamediv>
  <refname>DISCARD</refname>
<!--
  <refpurpose>discard session state</refpurpose>
-->
  <refpurpose>丢弃会话状态</refpurpose>
 </refnamediv>

 <refsynopsisdiv>
<synopsis>
DISCARD { ALL | PLANS | SEQUENCES | TEMPORARY | TEMP }
</synopsis>
 </refsynopsisdiv>

 <refsect1>
<!--
  <title>Description</title>
-->
  <title>描述</title>
<!--
  <para>
   <command>DISCARD</> releases internal resources associated with a
   database session.  This command is useful for partially or fully
   resetting the session's state.  There are several subcommands to
   release different types of resources; the <command>DISCARD ALL</>
   variant subsumes all the others, and also resets additional state.
  </para>
-->
  <para>
   <command>DISCARD</>释放与数据库会话相关的内部资源。
   这个命令对于部分或全部的重设会话的状态是有用的。
   有几个子命令释放不同类型的资源；<command>DISCARD ALL</>
   包含所有其他子命令，也重设额外的状态。
  </para>
 </refsect1>

 <refsect1>
<!--
  <title>Parameters</title>
-->
  <title>参数</title>

  <variablelist>

   <varlistentry>
    <term><literal>PLANS</literal></term>
    <listitem>
<!--
     <para>
      Releases all cached query plans, forcing re-planning to occur
      the next time the associated prepared statement is used.
     </para>
-->
     <para>
      释放所有缓存的查询计划，强制在下次使用相关准备好的语句时重新规划。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term><literal>SEQUENCES</literal></term>
    <listitem>
<!-- 
     <para>
      Discards all cached sequence-related state,
      including <function>currval()</>/<function>lastval()</>
      information and any preallocated sequence values that have not
      yet been returned by <function>nextval()</>.
      (See <xref linkend="sql-createsequence"> for a description of
      preallocated sequence values.)
     </para>
-->
<para>
丢弃所有缓存的序列相关的状态，包括<function>currval()</>/<function>lastval()</>
信息和任何至今还未通过<function>nextval()</>返回的预先分配的序列值。
（参阅<xref linkend="sql-createsequence">获取预先分配的序列值的描述。）
</para>
    </listitem>
   </varlistentry>

   <varlistentry>
<!-- 
    <term><literal>TEMPORARY</literal> or <literal>TEMP</literal></term>
-->
    <term><literal>TEMPORARY</literal> 或 <literal>TEMP</literal></term>

    <listitem>
<!--
     <para>
      Drops all temporary tables created in the current session.
     </para>
-->
     <para>
      删除在当前会话中创建的所有的临时表。
     </para>
    </listitem>
   </varlistentry>
   
   <varlistentry>
    <term><literal>ALL</literal></term>
    <listitem>
<!--
     <para>
      Releases all temporary resources associated with the current
      session and resets the session to its initial state.
      Currently, this has the same effect as executing the following sequence
      of statements:
<programlisting>
SET SESSION AUTHORIZATION DEFAULT;
RESET ALL;
DEALLOCATE ALL;
CLOSE ALL;
UNLISTEN *;
SELECT pg_advisory_unlock_all();
DISCARD PLANS;
DISCARD SEQUENCES;
DISCARD TEMP;
</programlisting></para>
-->
     <para>
      释放所有与当前会话相关的临时资源并重置到其初始状态。
      当前，这与执行以下语句序列有相同的效果:
<programlisting>
SET SESSION AUTHORIZATION DEFAULT;
RESET ALL;
DEALLOCATE ALL;
CLOSE ALL;
UNLISTEN *;
SELECT pg_advisory_unlock_all();
DISCARD PLANS;
DISCARD SEQUENCES;
DISCARD TEMP;
</programlisting></para>

    </listitem>
   </varlistentry>

  </variablelist>
 </refsect1>

 <refsect1>
<!--
  <title>Notes</title>
-->
  <title>说明</title>
<!--
   <para>
    <command>DISCARD ALL</> cannot be executed inside a transaction block.
   </para>
-->
   <para>
    <command>DISCARD ALL</>在一个事务内部模块中不能被执行。
   </para>
 </refsect1>

 <refsect1>
<!--
  <title>Compatibility</title>
-->
  <title>兼容性</title>
<!--
  <para>
   <command>DISCARD</command> is a <productname>PostgreSQL</productname> extension.
  </para>
-->
  <para>
   <command>DISCARD</command>是一个<productname>PostgreSQL</productname>扩展。
  </para>
 </refsect1>
</refentry>
